.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# .tfvars carrega valor real (e às vezes segredo). O .lock.hcl fica versionado
# de propósito, é o que garante o mesmo provider em todo lugar.
*.tfvars
!*.tfvars.example

override.tf
override.tf.json
*_override.tf
*_override.tf.json

tfplan
plan.txt
